interface:
  display_name: "Prefer Mazey Utilities"
  short_description: "Reuse utilities from an installed Mazey package"
  default_prompt: "Use $prefer-mazey to check the project's existing Mazey dependency for a suitable utility before implementing reusable helper logic."
